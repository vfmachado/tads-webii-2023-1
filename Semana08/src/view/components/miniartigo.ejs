<div class="w3-row">
    <div class="w3-twothird w3-container">
        <a href="/midias/<%= id %>">
        <h1 class="w3-text-teal">

            <%= title %>
        </h1>
        </a>
        <p>
            <% if (locals.text) { %>
                <%= text %>
                    <% } else { %>
                        ARTIGO SEM TEXTO
                        <% } %>
        </p>
    </div>

</div>